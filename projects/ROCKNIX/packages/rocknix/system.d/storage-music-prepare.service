[Unit]
Description=Create /storage/music mountpoint
After=storage.mount
Requires=storage.mount
Before=storage-music.mount
DefaultDependencies=no

[Service]
Type=oneshot
ExecStart=/bin/mkdir -p /storage/music
RemainAfterExit=yes

[Install]
WantedBy=storage-music.mount
