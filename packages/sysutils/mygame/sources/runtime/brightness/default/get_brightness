#!/bin/bash

if [ "$BL_TYPE" = "TYPE1" ]; then
    brightness=$(grep -oP 'brightness=\K[0-9]+' /sys/class/disp/disp/attr/sys)
    echo "$brightness"
elif [ "$BL_TYPE" = "TYPE2" ]; then
    cat "$SYSFS_BL_BRIGHTNESS"
else
    echo "155"
fi
